# Пример файла переменных окружения
# Скопируйте этот файл в .env и заполните реальными значениями
# Файл .env должен быть исключен из системы контроля версий (уже добавлен в .gitignore)

# Секретный ключ Django (обязательно)
# Сгенерируйте новый ключ командой: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-CHANGE-THIS-TO-A-RANDOM-SECRET-KEY

# Режим отладки (True для разработки, False для production)
DEBUG=True

# Разрешенные хосты (через запятую, без пробелов)
# Для разработки можно использовать: *
# Для production укажите конкретные домены: example.com,www.example.com
ALLOWED_HOSTS=*

# Настройки базы данных
# Для SQLite (разработка) - ничего не указывайте, будет использован SQLite по умолчанию
# DATABASE_URL=sqlite:///db.sqlite3

# Для PostgreSQL (production):
# Вариант 1: Использование DATABASE_URL
# DATABASE_URL=postgresql://user:password@localhost:5432/fin_count

# Вариант 2: Использование отдельных переменных
# DB_NAME=fin_count
# DB_USER=your_user
# DB_PASSWORD=your_password
# DB_HOST=localhost
# DB_PORT=5432

# Настройки системы финансового учета
# Глобальный префикс номеров документов (2 символа)
DOCUMENT_NUMBER_PREFIX=SC

# Дополнительные настройки (опционально)
# DJANGO_SETTINGS_MODULE=fin_count.settings
# STATIC_ROOT=/path/to/staticfiles
# MEDIA_ROOT=/path/to/media

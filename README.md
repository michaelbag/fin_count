# fin_count

Система финансового учета для ведения кассовых остатков, подотчетных средств и авансовых отчетов.

## 📋 Описание

**fin_count** — это система финансового учета, разработанная на Django, предназначенная для:

- Учета остатков денежных средств по кассам в различных валютах
- Учета остатков по выданным авансам сотрудникам
- Оформления операций документами (оприходование, расход, выдача подотчетных средств, авансовые отчеты)
- Ведения общего журнала операций со ссылками на документы, физические лица, валюты, статьи расходов и доходов
- Ведения справочника статей расходов и доходов

## 🚀 Статус проекта

**Статус:** В разработке документации  
**Версия:** 0.0.1

## 🛠 Технологический стек

- **Backend:** Django 5.2.8
- **База данных:** 
  - SQLite (для разработки)
  - PostgreSQL (для промышленного применения)
- **Язык:** Python 3.11+
- **Интерфейс:** Django Admin + пользовательские формы

## 📦 Требования

- Python 3.11+
- Django 5.2.8
- PostgreSQL (для промышленного применения, опционально)

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/michaelbag/fin_count.git
cd fin_count
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
```

### 3. Активация виртуального окружения

**Linux/macOS:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

**Важно:** Все конфигурационные данные (секретные ключи, настройки базы данных) должны храниться в переменных окружения, а не в коде.

1. Скопируйте пример файла переменных окружения:
```bash
cp .env.example .env
```

2. Отредактируйте файл `.env` и заполните реальными значениями:
```bash
# Сгенерируйте новый секретный ключ
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'

# Откройте .env в редакторе и вставьте сгенерированный ключ в SECRET_KEY
```

**Минимальная конфигурация для разработки:**
```env
SECRET_KEY=your-generated-secret-key-here
DEBUG=True
ALLOWED_HOSTS=*
DOCUMENT_NUMBER_PREFIX=SC
```

**Для production обязательно измените:**
- `DEBUG=False`
- `ALLOWED_HOSTS=your-domain.com,www.your-domain.com`
- Настройте подключение к PostgreSQL (см. ниже)

### 6. Настройка базы данных

**Для разработки (SQLite):**

База данных SQLite создается автоматически при первом запуске миграций. Никаких дополнительных настроек не требуется (не указывайте DATABASE_URL в .env).

**Для промышленного применения (PostgreSQL):**

1. Установите драйвер PostgreSQL:
```bash
pip install psycopg2-binary
```

2. Создайте базу данных в PostgreSQL:
```sql
CREATE DATABASE fin_count;
CREATE USER your_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE fin_count TO your_user;
```

3. Настройте переменные окружения в `.env`:

**Вариант 1: Использование DATABASE_URL (рекомендуется)**
```env
DATABASE_URL=postgresql://your_user:your_password@localhost:5432/fin_count
```

**Вариант 2: Использование отдельных переменных**
```env
DB_NAME=fin_count
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

### 7. Применение миграций

```bash
python manage.py migrate
```

### 8. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 9. Запуск сервера разработки

```bash
python manage.py runserver 0.0.0.0:8000
```

Приложение будет доступно по адресу: http://localhost:8000

Админ-панель: http://localhost:8000/admin

## 📚 Документация

Подробное техническое задание находится в файле [TECHNICAL_SPECIFICATION.md](TECHNICAL_SPECIFICATION.md)

## 🏗 Архитектура

Система построена на основе 4 абстрактных базовых моделей:

1. **BaseDocument** — базовая модель для всех документов системы
2. **BaseOperationRegister** — базовая модель для регистра операций (журнала операций)
3. **BaseAccumulationRegister** — базовая модель для регистров накоплений (остатков)
4. **BaseReference** — базовая модель для всех справочников системы

## 📁 Структура проекта

```
fin_count/
├── accounting/          # Приложение учета
│   ├── abstract_models.py  # Абстрактные базовые модели
│   ├── models.py       # Модели данных
│   ├── admin.py        # Настройки Django Admin
│   ├── views.py        # Представления
│   ├── forms.py        # Формы
│   └── urls.py         # URL-маршруты
├── fin_count/          # Настройки проекта
│   ├── settings.py     # Настройки Django
│   ├── urls.py         # Главные URL-маршруты
│   └── __version__.py  # Версия проекта
├── manage.py           # Управляющий скрипт Django
├── TECHNICAL_SPECIFICATION.md  # Техническое задание
└── README.md           # Этот файл
```

## 🔐 Безопасность

- Валидация данных на уровне моделей
- Защита от SQL-инъекций (ORM Django)
- Контроль доступа через Django Admin
- **Безопасное хранение конфигурации:** Все секретные данные (SECRET_KEY, пароли БД) хранятся в переменных окружения через файл `.env`
- Файл `.env` исключен из системы контроля версий (добавлен в `.gitignore`)
- Для production рекомендуется использовать системные переменные окружения вместо `.env` файла

## 🌍 Интернационализация

Интерфейс системы полностью на русском языке.

## 📝 Лицензия

Проект находится в разработке.

## 👥 Авторы

- **michaelbag** - [GitHub](https://github.com/michaelbag)

## 🔗 Ссылки

- **Репозиторий:** https://github.com/michaelbag/fin_count
- **Техническое задание:** [TECHNICAL_SPECIFICATION.md](TECHNICAL_SPECIFICATION.md)

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории проекта.

---

**Версия:** 0.0.1  
**Последнее обновление:** 2024

